{
  "name": "react-stars",
  "version": "1.0.0",
  "description": "Simple star rating component for your React projects",
  "main": "index.js",

  "babel": {
    "presets": ["react", "es2015"]
  },

  "scripts": {
    "build": "babel src/ReactStars.js --out-file dist/index.js",
    "dev": "babel src/ReactStars.js --out-file dist/index.js --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "keywords": [
    "star",
    "rating",
    "react",
    "star",
    "rating",
    "component",
    "raty"
  ],

  "author": "Oleg Berman",

  "license": "ISC",

  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-react": "^6.5.0"
  }

}
